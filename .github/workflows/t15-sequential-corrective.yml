name: File 08 T15 Sequential Corrective Audit
on:
  push:
    branches:
      - codex/file08-new-governing-plans-completion-2026
    paths:
      - .github/workflows/t15-sequential-corrective.yml
permissions:
  contents: write
concurrency:
  group: file08-t15-sequential-corrective
  cancel-in-progress: false
jobs:
  review-fix-retest:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: codex/file08-new-governing-plans-completion-2026
          fetch-depth: 0
      - uses: shivammathur/setup-php@7c071dfe9dc99bdf297fa79cb49ea005b9fcadbc
        with:
          php-version: '8.3'
          coverage: none
          extensions: zip
      - name: Configure sequential audit
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'File 08 Corrective Auditor'
          git config user.email 'actions@users.noreply.github.com'
          cat > /tmp/t15_patch.py <<'PY'
          from pathlib import Path
          import re, sys

          root = Path('.')

          def read(path):
              return (root / path).read_text()
          def write(path, data):
              (root / path).write_text(data)
          def once(path, old, new):
              s=read(path)
              n=s.count(old)
              if n != 1:
                  raise SystemExit(f'{path}: expected one match, found {n}: {old[:90]!r}')
              write(path, s.replace(old,new,1))
          def many(path, old, new, count):
              s=read(path)
              n=s.count(old)
              if n != count:
                  raise SystemExit(f'{path}: expected {count} matches, found {n}: {old[:90]!r}')
              write(path, s.replace(old,new))
          def rx(path, pattern, replacement):
              s=read(path)
              out,n=re.subn(pattern,replacement,s,count=1,flags=re.S)
              if n != 1:
                  raise SystemExit(f'{path}: regex expected one match, found {n}: {pattern[:90]!r}')
              write(path,out)

          r=sys.argv[1]

          if r == 'R01':
              p='includes/class-swc-helpers.php'
              old="\t\t$rows = $wpdb->get_results( $wpdb->prepare( $sql, absint( $doctor_id ), $from, $to, self::TYPE, absint( $exclude_id ) ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared\n\t\tforeach ( (array) $rows as $row ) {"
              new="\t\t$rows = $wpdb->get_results( $wpdb->prepare( $sql, absint( $doctor_id ), $from, $to, self::TYPE, absint( $exclude_id ) ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared\n\t\t// Fail closed: an unreadable conflict set must never be treated as an empty schedule.\n\t\tif ( null === $rows || '' !== (string) $wpdb->last_error ) { return true; }\n\t\tforeach ( (array) $rows as $row ) {"
              once(p,old,new)
          elif r == 'R02':
              p='includes/class-wca-service.php'
              replacement='''\tprivate static function has_active_hold( $doctor_id, $start_utc, $end_utc, $ignore_idempotency_key = '' ) {
\t\tglobal $wpdb;
\t\t$table = WCA_Schema::tables()['slot_holds'];
\t\tif ( $ignore_idempotency_key ) {
\t\t\t$value = $wpdb->get_var( $wpdb->prepare( "SELECT id FROM {$table} WHERE doctor_user_id=%d AND status IN ('held','booked') AND idempotency_key<>%s AND expires_at>%s AND start_utc<%s AND end_utc>%s LIMIT 1", absint( $doctor_id ), sanitize_text_field( $ignore_idempotency_key ), WCA_Repository::now(), $end_utc, $start_utc ) );
\t\t} else {
\t\t\t$value = $wpdb->get_var( $wpdb->prepare( "SELECT id FROM {$table} WHERE doctor_user_id=%d AND status IN ('held','booked') AND expires_at>%s AND start_utc<%s AND end_utc>%s LIMIT 1", absint( $doctor_id ), WCA_Repository::now(), $end_utc, $start_utc ) );
\t\t}
\t\t// Fail closed: an unreadable hold ledger means availability is unknown, not free.
\t\tif ( '' !== (string) $wpdb->last_error ) { return true; }
\t\treturn (bool) $value;
\t}

\t/** Reproject'''
              rx(p,r"\tprivate static function has_active_hold\(.*?\n\t\}\n\n\t/\*\* Reproject",replacement)
          elif r == 'R03':
              p='includes/class-wca-repository.php'
              old='''\t\t$existing = $wpdb->get_row( $wpdb->prepare( "SELECT * FROM {$table} WHERE idempotency_key=%s LIMIT 1", $idempotency_key ), ARRAY_A );
\t\t$existing = $replay( $existing );'''
              new='''\t\t$existing = $wpdb->get_row( $wpdb->prepare( "SELECT * FROM {$table} WHERE idempotency_key=%s LIMIT 1", $idempotency_key ), ARRAY_A );
\t\tif ( null === $existing && '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_slot_hold_read_failed', __( 'The slot-hold ledger could not be read safely.', 'worldwide-clinic-appointments' ), array( 'status' => 503 ) ); }
\t\t$existing = $replay( $existing );'''
              many(p,old,new,2)
              old2='''\t\t\t$conflict = $wpdb->get_var( $wpdb->prepare(
\t\t\t\t"SELECT id FROM {$table} WHERE doctor_user_id=%d AND status IN ('held','booked') AND expires_at>%s AND start_utc<%s AND end_utc>%s LIMIT 1",
\t\t\t\t$doctor_id, self::now(), $end, $start
\t\t\t) );
\t\t\t$duration = max( 1, (int) round( ( strtotime( $end . ' UTC' ) - strtotime( $start . ' UTC' ) ) / 60 ) );'''
              new2='''\t\t\t$conflict = $wpdb->get_var( $wpdb->prepare(
\t\t\t\t"SELECT id FROM {$table} WHERE doctor_user_id=%d AND status IN ('held','booked') AND expires_at>%s AND start_utc<%s AND end_utc>%s LIMIT 1",
\t\t\t\t$doctor_id, self::now(), $end, $start
\t\t\t) );
\t\t\tif ( '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_slot_conflict_query_failed', __( 'Current slot conflicts could not be verified safely.', 'worldwide-clinic-appointments' ), array( 'status' => 503 ) ); }
\t\t\t$duration = max( 1, (int) round( ( strtotime( $end . ' UTC' ) - strtotime( $start . ' UTC' ) ) / 60 ) );'''
              once(p,old2,new2)
          elif r == 'R04':
              p='includes/class-swc-helpers.php'
              old='''\t\tif ( false === $result ) { return true; }
\t\t$hits = 1 === (int) $result ? 1 : (int) $wpdb->get_var( 'SELECT LAST_INSERT_ID()' ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\treturn $hits > max( 1, $limit );'''
              new='''\t\tif ( false === $result ) { return true; }
\t\tif ( 1 === (int) $result ) {
\t\t\t$hits = 1;
\t\t} else {
\t\t\t$hits_raw = $wpdb->get_var( 'SELECT LAST_INSERT_ID()' ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\t// Fail closed if the atomic counter increment succeeded but its authoritative count cannot be read.
\t\t\tif ( null === $hits_raw || '' !== (string) $wpdb->last_error ) { return true; }
\t\t\t$hits = (int) $hits_raw;
\t\t}
\t\treturn $hits > max( 1, $limit );'''
              once(p,old,new)
          elif r == 'R05':
              p='includes/class-wca-future24.php'
              old='''\t\t\t$count = (int) $wpdb->get_var( $wpdb->prepare( "SELECT COUNT(*) FROM {$table} WHERE feature_id='F08-FUT-04' AND parent_ref=%s AND status='reserved' AND (expires_at IS NULL OR expires_at>%s) AND starts_at<%s AND ends_at>%s", strtolower( $resource_ref ), WCA_Repository::now(), $end, $start ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( $count >= max( 1, absint( $resource['capacity'] ) ) ) { return new WP_Error( 'wca_resource_conflict', __( 'The resource is already at capacity for this time.', 'worldwide-clinic-appointments' ), array( 'status' => 409 ) ); }'''
              new='''\t\t\t$count_raw = $wpdb->get_var( $wpdb->prepare( "SELECT COUNT(*) FROM {$table} WHERE feature_id='F08-FUT-04' AND parent_ref=%s AND status='reserved' AND (expires_at IS NULL OR expires_at>%s) AND starts_at<%s AND ends_at>%s", strtolower( $resource_ref ), WCA_Repository::now(), $end, $start ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( null === $count_raw || '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_resource_capacity_read', __( 'Current resource capacity could not be verified safely.', 'worldwide-clinic-appointments' ), array( 'status' => 503 ) ); }
\t\t\t$count = (int) $count_raw;
\t\t\tif ( $count >= max( 1, absint( $resource['capacity'] ) ) ) { return new WP_Error( 'wca_resource_conflict', __( 'The resource is already at capacity for this time.', 'worldwide-clinic-appointments' ), array( 'status' => 409 ) ); }'''
              once(p,old,new)
          elif r == 'R06':
              p='includes/class-wca-future24.php'
              old='''\t\t\t$exists = $wpdb->get_var( $wpdb->prepare( "SELECT public_ref FROM {$table} WHERE feature_id='F08-FUT-05' AND parent_ref=%s AND subject_user_id=%d AND status='group_member' LIMIT 1", strtolower( $session_ref ), $context['patient_user_id'] ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( $exists ) { return self::public_record( self::get_record( $exists, 'F08-FUT-05' ) ); }
\t\t\t$count = (int) $wpdb->get_var( $wpdb->prepare( "SELECT COUNT(*) FROM {$table} WHERE feature_id='F08-FUT-05' AND parent_ref=%s AND status='group_member'", strtolower( $session_ref ) ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( $count >= absint( $session['capacity'] ) ) { return new WP_Error( 'wca_group_full', __( 'This group appointment is full.', 'worldwide-clinic-appointments' ), array( 'status' => 409 ) ); }'''
              new='''\t\t\t$existing_member = $wpdb->get_row( $wpdb->prepare( "SELECT * FROM {$table} WHERE feature_id='F08-FUT-05' AND parent_ref=%s AND subject_user_id=%d AND status='group_member' LIMIT 1", strtolower( $session_ref ), $context['patient_user_id'] ), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( null === $existing_member && '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_group_membership_read', __( 'Current group membership could not be verified safely.', 'worldwide-clinic-appointments' ), array( 'status' => 503 ) ); }
\t\t\tif ( $existing_member ) { return self::public_record( $existing_member ); }
\t\t\t$count_raw = $wpdb->get_var( $wpdb->prepare( "SELECT COUNT(*) FROM {$table} WHERE feature_id='F08-FUT-05' AND parent_ref=%s AND status='group_member'", strtolower( $session_ref ) ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( null === $count_raw || '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_group_capacity_read', __( 'Current group capacity could not be verified safely.', 'worldwide-clinic-appointments' ), array( 'status' => 503 ) ); }
\t\t\t$count = (int) $count_raw;
\t\t\tif ( $count >= absint( $session['capacity'] ) ) { return new WP_Error( 'wca_group_full', __( 'This group appointment is full.', 'worldwide-clinic-appointments' ), array( 'status' => 409 ) ); }'''
              once(p,old,new)
          elif r == 'R07':
              p='includes/class-wca-future24.php'
              old='''\t\t$row = $wpdb->get_row( $wpdb->prepare( "SELECT payload_json FROM {$table} WHERE feature_id='F08-FUT-07' AND clinic_id=%d AND status='policy_active' ORDER BY id DESC LIMIT 1", absint( $clinic['id'] ) ), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\tif ( ! $row ) { return true; }'''
              new='''\t\t$row = $wpdb->get_row( $wpdb->prepare( "SELECT payload_json FROM {$table} WHERE feature_id='F08-FUT-07' AND clinic_id=%d AND status='policy_active' ORDER BY id DESC LIMIT 1", absint( $clinic['id'] ) ), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t// Fail closed: a policy read error is not equivalent to “no active policy”.
\t\tif ( null === $row && '' !== (string) $wpdb->last_error ) { return false; }
\t\tif ( ! $row ) { return true; }'''
              once(p,old,new)
              old2='''\t\t\t\t\t$appointment_branch = $branch_id ? strtolower( (string) $wpdb->get_var( $wpdb->prepare( "SELECT public_ref FROM {$branches_table} WHERE id=%d LIMIT 1", $branch_id ) ) ) : ''; // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\t\t\tif ( $appointment_branch && $slot_branch && ! hash_equals( $appointment_branch, $slot_branch ) ) {'''
              new2='''\t\t\t\t\t$appointment_branch = '';
\t\t\t\t\tif ( $branch_id ) {
\t\t\t\t\t\t$branch_raw = $wpdb->get_var( $wpdb->prepare( "SELECT public_ref FROM {$branches_table} WHERE id=%d LIMIT 1", $branch_id ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\t\t\t\tif ( '' !== (string) $wpdb->last_error ) { return false; }
\t\t\t\t\t\t$appointment_branch = strtolower( (string) $branch_raw );
\t\t\t\t\t}
\t\t\t\t\tif ( $appointment_branch && $slot_branch && ! hash_equals( $appointment_branch, $slot_branch ) ) {'''
              once(p,old2,new2)
          elif r == 'R08':
              p='includes/class-wca-future24.php'
              replacement='''\tprivate static function external_busy_conflict_ref( $practitioner_ref, $start, $end ) {
\t\tglobal $wpdb; $start=self::utc($start); $end=self::utc($end); $practitioner_ref=sanitize_text_field($practitioner_ref); if(!$practitioner_ref||!$start||!$end){return false;} $table=self::tables()['records'];
\t\t$value = $wpdb->get_var( $wpdb->prepare( "SELECT id FROM {$table} WHERE feature_id='F08-FUT-22' AND parent_ref=%s AND status='busy' AND (expires_at IS NULL OR expires_at>%s) AND starts_at<%s AND ends_at>%s LIMIT 1", $practitioner_ref, WCA_Repository::now(), $end, $start ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t// Fail closed: an unreadable external-busy projection must block scheduling until reconciled.
\t\tif ( '' !== (string) $wpdb->last_error ) { return true; }
\t\treturn (bool) $value;
\t}

\t/* FUT-23 */'''
              rx(p,r"\tprivate static function external_busy_conflict_ref\(.*?\n\t\}\n\n\t/\* FUT-23 \*/",replacement)
          elif r == 'R09':
              p='includes/class-wca-future24.php'
              old='''\t\t\t$existing = $wpdb->get_row( $wpdb->prepare( "SELECT * FROM {$table} WHERE feature_id='F08-FUT-01' AND clinic_id=%d AND subject_user_id=%d AND parent_ref=%s AND status='waiting' AND (expires_at IS NULL OR expires_at>%s) ORDER BY id DESC LIMIT 1", $clinic_id, $context['patient_user_id'], $fingerprint, WCA_Repository::now() ), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( $existing ) { return self::public_record( $existing ); }'''
              new=old.split('\n')[0]+'''\n\t\t\tif ( null === $existing && '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_waitlist_dedupe_read', __( 'Current waitlist state could not be verified safely.', 'worldwide-clinic-appointments' ), array( 'status' => 503 ) ); }
\t\t\tif ( $existing ) { return self::public_record( $existing ); }'''
              once(p,old,new)
              old2='''\t\t\t$existing = $wpdb->get_row( $wpdb->prepare( "SELECT * FROM {$table} WHERE feature_id='F08-FUT-02' AND clinic_id=%d AND subject_user_id=%d AND parent_ref=%s AND status='open' AND (expires_at IS NULL OR expires_at>%s) ORDER BY id DESC LIMIT 1", $clinic_id, $context['patient_user_id'], $fingerprint, WCA_Repository::now() ), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( $existing ) { return self::public_record( $existing ); }'''
              new2=old2.split('\n')[0]+'''\n\t\t\tif ( null === $existing && '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_windows_dedupe_read', __( 'Current flexible-window state could not be verified safely.', 'worldwide-clinic-appointments' ), array( 'status' => 503 ) ); }
\t\t\tif ( $existing ) { return self::public_record( $existing ); }'''
              once(p,old2,new2)
          elif r == 'R10':
              p='includes/class-wca-future24.php'
              old='''\t\t\t\t\t$duplicate = $wpdb->get_var( $wpdb->prepare( "SELECT public_ref FROM {$table} WHERE feature_id='F08-FUT-01' AND parent_ref=%s AND status='offer_pending' AND starts_at=%s AND ends_at=%s AND (expires_at IS NULL OR expires_at>%s) LIMIT 1", (string) $wait['public_ref'], $start, $end, WCA_Repository::now() ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\t\t\tif ( $duplicate ) { return false; }'''
              new='''\t\t\t\t\t$duplicate = $wpdb->get_var( $wpdb->prepare( "SELECT public_ref FROM {$table} WHERE feature_id='F08-FUT-01' AND parent_ref=%s AND status='offer_pending' AND starts_at=%s AND ends_at=%s AND (expires_at IS NULL OR expires_at>%s) LIMIT 1", (string) $wait['public_ref'], $start, $end, WCA_Repository::now() ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\t\t\tif ( '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_waitlist_offer_dedupe_read', __( 'Existing waitlist offers could not be verified safely.', 'worldwide-clinic-appointments' ), array( 'status' => 503 ) ); }
\t\t\t\t\tif ( $duplicate ) { return false; }'''
              once(p,old,new)
          elif r == 'R11':
              p='includes/class-wca-future24.php'
              old='''\t\t\t$existing = $wpdb->get_row( $wpdb->prepare( "SELECT * FROM {$table} WHERE feature_id='F08-FUT-15' AND appointment_id=%d AND subject_user_id=%d AND status='arrived' AND (expires_at IS NULL OR expires_at>%s) ORDER BY id DESC LIMIT 1", $id, $actor_id, WCA_Repository::now() ), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( $existing ) { return self::public_record( $existing ); }'''
              new=old.split('\n')[0]+'''\n\t\t\tif ( null === $existing && '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_arrival_dedupe_read', __( 'Current arrival state could not be verified safely.', 'worldwide-clinic-appointments' ), array( 'status' => 503 ) ); }
\t\t\tif ( $existing ) { return self::public_record( $existing ); }'''
              once(p,old,new)
          elif r == 'R12':
              p='includes/class-wca-future24.php'
              old='''\t\t\t$existing=$wpdb->get_row($wpdb->prepare("SELECT * FROM {$table} WHERE feature_id='F08-FUT-18' AND appointment_id=%d AND subject_user_id=%d AND status='participant_active' AND (expires_at IS NULL OR expires_at>%s) ORDER BY id DESC LIMIT 1",$id,$subject_user_id,WCA_Repository::now()),ARRAY_A); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif($existing){$payload=json_decode((string)$existing['payload_json'],true); if(is_array($payload)&&sanitize_key($payload['role'] ?? '')===$role){return self::public_record($existing);} return new WP_Error('wca_support_participant_conflict',__('This participant already has an active appointment role.','worldwide-clinic-appointments'),array('status'=>409));}'''
              new=old.split('\n')[0]+'''\n\t\t\tif(null===$existing && ''!==(string)$wpdb->last_error){return new WP_Error('wca_support_participant_dedupe_read',__('Current support-participant state could not be verified safely.','worldwide-clinic-appointments'),array('status'=>503));}
\t\t\tif($existing){$payload=json_decode((string)$existing['payload_json'],true); if(is_array($payload)&&sanitize_key($payload['role'] ?? '')===$role){return self::public_record($existing);} return new WP_Error('wca_support_participant_conflict',__('This participant already has an active appointment role.','worldwide-clinic-appointments'),array('status'=>409));}'''
              once(p,old,new)
          elif r == 'R13':
              p='includes/class-wca-future24.php'
              old='''\t\t\t\t$existing=$wpdb->get_row($wpdb->prepare("SELECT * FROM {$table} WHERE feature_id='F08-FUT-19' AND appointment_id=%d AND status='room_requested' AND (expires_at IS NULL OR expires_at>%s) ORDER BY id DESC LIMIT 1",$id,WCA_Repository::now()),ARRAY_A); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\t\tif($existing){return self::public_record($existing);}'''
              new=old.split('\n')[0]+'''\n\t\t\t\tif(null===$existing && ''!==(string)$wpdb->last_error){return new WP_Error('wca_virtual_room_dedupe_read',__('Current virtual-room request state could not be verified safely.','worldwide-clinic-appointments'),array('status'=>503));}
\t\t\t\tif($existing){return self::public_record($existing);}'''
              once(p,old,new)
          elif r == 'R14':
              p='includes/class-wca-future24.php'
              old='''\t\t\t$rows = (array) $wpdb->get_results( $wpdb->prepare(
\t\t\t\t"SELECT {$columns} FROM {$table} WHERE feature_id=%s AND clinic_id=%d AND status=%s AND id>%d ORDER BY id ASC LIMIT %d",
\t\t\t\t$feature_id, absint( $clinic_id ), $status, $cursor, $batch
\t\t\t), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tforeach ( $rows as $row ) { $cursor = max( $cursor, absint( $row['id'] ?? 0 ) ); $out[] = $row; }'''
              new='''\t\t\t$rows_raw = $wpdb->get_results( $wpdb->prepare(
\t\t\t\t"SELECT {$columns} FROM {$table} WHERE feature_id=%s AND clinic_id=%d AND status=%s AND id>%d ORDER BY id ASC LIMIT %d",
\t\t\t\t$feature_id, absint( $clinic_id ), $status, $cursor, $batch
\t\t\t), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( null === $rows_raw && '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_future24_policy_read', __( 'Current Future24 policy data could not be read safely.', 'worldwide-clinic-appointments' ), array( 'status' => 503 ) ); }
\t\t\t$rows = (array) $rows_raw;
\t\t\tforeach ( $rows as $row ) { $cursor = max( $cursor, absint( $row['id'] ?? 0 ) ); $out[] = $row; }'''
              once(p,old,new)
              old2="\t\t$rows = array_reverse( self::feature_rows_for_clinic( 'F08-FUT-11', $clinic_id, 'template_active', 'questionnaire' ) );"
              new2="\t\t$rows = self::feature_rows_for_clinic( 'F08-FUT-11', $clinic_id, 'template_active', 'questionnaire' );\n\t\tif ( is_wp_error( $rows ) ) { return $rows; }\n\t\t$rows = array_reverse( $rows );"
              once(p,old2,new2)
              old3="\t\t$rules = array_reverse( self::feature_rows_for_clinic( 'F08-FUT-13', $clinic_id, 'rule_active', 'payload' ) );"
              new3="\t\t$rules = self::feature_rows_for_clinic( 'F08-FUT-13', $clinic_id, 'rule_active', 'payload' );\n\t\tif ( is_wp_error( $rules ) ) { return $rules; }\n\t\t$rules = array_reverse( $rules );"
              once(p,old3,new3)
              old4="\t\t$prereq = self::prerequisite_state( $id );"
              new4="\t\t$prereq = self::prerequisite_state( $id );\n\t\tif ( is_wp_error( $prereq ) ) { return $prereq; }"
              once(p,old4,new4)
          elif r == 'R15':
              p='includes/class-wca-privacy.php'
              old="\t\t$ids = self::appointment_ids_after( $user_id, $cursor, self::ERASE_BATCH );\n\t\t$last_id = $cursor;"
              new="\t\t$ids = self::appointment_ids_after( $user_id, $cursor, self::ERASE_BATCH );\n\t\tif ( is_wp_error( $ids ) ) { $messages[] = __( 'Appointment privacy erasure could not read the affected record set safely and will retry.', 'worldwide-clinic-appointments' ); $done = false; $ids = array(); }\n\t\t$last_id = $cursor;"
              once(p,old,new)
              old2="\t\tif ( self::appointment_ids_after( $user_id, $last_id, 1 ) ) { $done = false; } else { delete_transient( $cursor_key ); }"
              new2="\t\t$appointment_more = self::appointment_ids_after( $user_id, $last_id, 1 );\n\t\tif ( is_wp_error( $appointment_more ) ) { $messages[] = __( 'Appointment privacy erasure could not verify completion safely and will retry.', 'worldwide-clinic-appointments' ); $done = false; } elseif ( $appointment_more ) { $done = false; } else { delete_transient( $cursor_key ); }"
              once(p,old2,new2)
              old3='''\t\t\t$rows = (array) $wpdb->get_results(
\t\t\t\t$wpdb->prepare(
\t\t\t\t\t"SELECT * FROM {$table} WHERE (actor_user_id=%d OR subject_user_id=%d) AND id>%d ORDER BY id ASC LIMIT %d",
\t\t\t\t\t$user_id, $user_id, $cursor, self::ERASE_BATCH
\t\t\t\t),
\t\t\t\tARRAY_A
\t\t\t); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared'''
              new3='''\t\t\t$rows_raw = $wpdb->get_results(
\t\t\t\t$wpdb->prepare(
\t\t\t\t\t"SELECT * FROM {$table} WHERE (actor_user_id=%d OR subject_user_id=%d) AND id>%d ORDER BY id ASC LIMIT %d",
\t\t\t\t\t$user_id, $user_id, $cursor, self::ERASE_BATCH
\t\t\t\t),
\t\t\t\tARRAY_A
\t\t\t); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( null === $rows_raw && '' !== (string) $wpdb->last_error ) { $messages[] = __( 'Future24 privacy erasure could not read the affected record set safely and will retry.', 'worldwide-clinic-appointments' ); $done = false; $rows_raw = array(); }
\t\t\t$rows = (array) $rows_raw;'''
              once(p,old3,new3)
              old4="\t\t\t$more = $wpdb->get_var( $wpdb->prepare( \"SELECT id FROM {$table} WHERE (actor_user_id=%d OR subject_user_id=%d) AND id>%d ORDER BY id ASC LIMIT 1\", $user_id, $user_id, $last ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared\n\t\t\tif ( $more ) { $done = false; } else { delete_transient( $cursor_key ); }"
              new4="\t\t\t$more = $wpdb->get_var( $wpdb->prepare( \"SELECT id FROM {$table} WHERE (actor_user_id=%d OR subject_user_id=%d) AND id>%d ORDER BY id ASC LIMIT 1\", $user_id, $user_id, $last ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared\n\t\t\tif ( null === $more && '' !== (string) $wpdb->last_error ) { $messages[] = __( 'Future24 privacy erasure could not verify completion safely and will retry.', 'worldwide-clinic-appointments' ); $done = false; } elseif ( $more ) { $done = false; } else { delete_transient( $cursor_key ); }"
              once(p,old4,new4)
              old5='''\t\t\t\t$rows = (array) $wpdb->get_results(
\t\t\t\t\t$wpdb->prepare( "SELECT * FROM {$table} WHERE expires_at IS NOT NULL AND expires_at<%s AND updated_at<%s AND id>%d ORDER BY id ASC LIMIT %d", WCA_Repository::now(), $cutoff, $cursor, $batch ),
\t\t\t\t\tARRAY_A
\t\t\t\t); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared'''
              new5='''\t\t\t\t$rows_raw = $wpdb->get_results(
\t\t\t\t\t$wpdb->prepare( "SELECT * FROM {$table} WHERE expires_at IS NOT NULL AND expires_at<%s AND updated_at<%s AND id>%d ORDER BY id ASC LIMIT %d", WCA_Repository::now(), $cutoff, $cursor, $batch ),
\t\t\t\t\tARRAY_A
\t\t\t\t); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\t\tif ( null === $rows_raw && '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_retention_future24_read', __( 'Future24 retention cleanup could not read expired records safely.', 'worldwide-clinic-appointments' ), array( 'status' => 500 ) ); }
\t\t\t\t$rows = (array) $rows_raw;'''
              once(p,old5,new5)
              pattern=r"\tprivate static function appointment_ids_after\( \$user_id, \$cursor, \$limit \) \{.*?\n\t\}\n\n\tpublic static function legal_hold"
              replacement='''\tprivate static function appointment_ids_after( $user_id, $cursor, $limit ) {
\t\tglobal $wpdb;
\t\t$user_id = absint( $user_id );
\t\t$cursor = absint( $cursor );
\t\t$limit = min( 500, max( 1, absint( $limit ) ) );
\t\t$sql = $wpdb->prepare(
\t\t\t"SELECT DISTINCT p.ID
\t\t\t FROM {$wpdb->posts} p
\t\t\t INNER JOIN {$wpdb->postmeta} pm ON pm.post_id=p.ID
\t\t\t WHERE p.post_type=%s
\t\t\t   AND p.post_status IN ('private','publish','draft')
\t\t\t   AND p.ID>%d
\t\t\t   AND ((pm.meta_key='_swc_patient_user_id' AND pm.meta_value=%d) OR (pm.meta_key='_swc_guardian_user_id' AND pm.meta_value=%d) OR (pm.meta_key='_swc_doctor_id' AND pm.meta_value=%d))
\t\t\t ORDER BY p.ID ASC LIMIT %d",
\t\t\tSWC_Helpers::TYPE,
\t\t\t$cursor,
\t\t\t$user_id,
\t\t\t$user_id,
\t\t\t$user_id,
\t\t\t$limit
\t\t);
\t\t$raw = $wpdb->get_col( $sql ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\tif ( null === $raw && '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_privacy_appointment_read', __( 'Appointment privacy records could not be read safely.', 'worldwide-clinic-appointments' ), array( 'status' => 500 ) ); }
\t\treturn array_map( 'absint', (array) $raw );
\t}

\tpublic static function legal_hold'''
              rx(p,pattern,replacement)
          elif r == 'R16':
              p='includes/class-wca-continuity-secure.php'
              old='''\t\t\t$intakes = (array) $wpdb->get_results( $wpdb->prepare( "SELECT id,public_ref,appointment_id FROM {$intake_table} WHERE updated_at<%s AND id>%d ORDER BY id ASC LIMIT %d", $intake_cutoff, $cursor, $batch ), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tforeach ( $intakes as $row ) {'''
              new='''\t\t\t$intakes_raw = $wpdb->get_results( $wpdb->prepare( "SELECT id,public_ref,appointment_id FROM {$intake_table} WHERE updated_at<%s AND id>%d ORDER BY id ASC LIMIT %d", $intake_cutoff, $cursor, $batch ), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( null === $intakes_raw && '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_intake_retention_read', __( 'Expired pre-visit records could not be read safely.', 'worldwide-clinic-appointments' ), array( 'status' => 500 ) ); }
\t\t\t$intakes = (array) $intakes_raw;
\t\t\tforeach ( $intakes as $row ) {'''
              once(p,old,new)
              old2='''\t\t\t$followups = (array) $wpdb->get_results( $wpdb->prepare( "SELECT id,public_ref,appointment_id,status FROM {$follow_table} WHERE updated_at<%s AND status IN ('completed','cancelled') AND id>%d ORDER BY id ASC LIMIT %d", $follow_cutoff, $cursor, $batch ), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tforeach ( $followups as $row ) {'''
              new2='''\t\t\t$followups_raw = $wpdb->get_results( $wpdb->prepare( "SELECT id,public_ref,appointment_id,status FROM {$follow_table} WHERE updated_at<%s AND status IN ('completed','cancelled') AND id>%d ORDER BY id ASC LIMIT %d", $follow_cutoff, $cursor, $batch ), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( null === $followups_raw && '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_followup_retention_read', __( 'Expired follow-up records could not be read safely.', 'worldwide-clinic-appointments' ), array( 'status' => 500 ) ); }
\t\t\t$followups = (array) $followups_raw;
\t\t\tforeach ( $followups as $row ) {'''
              once(p,old2,new2)
          elif r == 'R17':
              p='includes/class-wca-continuity-guards.php'
              old='''\t\t$current = $wpdb->get_row( $wpdb->prepare( "SELECT id,version FROM {$table} WHERE appointment_id=%d LIMIT 1", $appointment_id ), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\tif ( ! $current ) {
\t\t\treturn $result;
\t\t}'''
              new='''\t\t$current = $wpdb->get_row( $wpdb->prepare( "SELECT id,version FROM {$table} WHERE appointment_id=%d LIMIT 1", $appointment_id ), ARRAY_A ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\tif ( null === $current && '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_intake_version_read', __( 'Current pre-visit version could not be verified safely.', 'worldwide-clinic-appointments' ), array( 'status' => 503 ) ); }
\t\tif ( ! $current ) {
\t\t\treturn $result;
\t\t}'''
              once(p,old,new)
              old2='''\t\t$rows = (array) $wpdb->get_results(
\t\t\t$wpdb->prepare(
\t\t\t\t"SELECT scope,status,terms_version,granted_at,revoked_at FROM {$table} WHERE appointment_id=%d ORDER BY id ASC",
\t\t\t\t$appointment_id
\t\t\t),
\t\t\tARRAY_A
\t\t); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared'''
              new2='''\t\t$rows_raw = $wpdb->get_results(
\t\t\t$wpdb->prepare(
\t\t\t\t"SELECT scope,status,terms_version,granted_at,revoked_at FROM {$table} WHERE appointment_id=%d ORDER BY id ASC",
\t\t\t\t$appointment_id
\t\t\t),
\t\t\tARRAY_A
\t\t); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\tif ( null === $rows_raw && '' !== (string) $wpdb->last_error ) { return new WP_Error( 'wca_consent_state_read', __( 'Current consent state could not be read safely.', 'worldwide-clinic-appointments' ), array( 'status' => 503 ) ); }
\t\t$rows = (array) $rows_raw;'''
              once(p,old2,new2)
              old3='''\t\t\t$rows = (array) $wpdb->get_results(
\t\t\t\t$wpdb->prepare(
\t\t\t\t\t"SELECT id,public_ref,appointment_id FROM {$table} WHERE {$field}=%d AND id>%d ORDER BY id ASC LIMIT %d",
\t\t\t\t\t$user_id,
\t\t\t\t\t$cursor,
\t\t\t\t\tself::ERASE_BATCH
\t\t\t\t),
\t\t\t\tARRAY_A
\t\t\t); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared'''
              new3='''\t\t\t$rows_raw = $wpdb->get_results(
\t\t\t\t$wpdb->prepare(
\t\t\t\t\t"SELECT id,public_ref,appointment_id FROM {$table} WHERE {$field}=%d AND id>%d ORDER BY id ASC LIMIT %d",
\t\t\t\t\t$user_id,
\t\t\t\t\t$cursor,
\t\t\t\t\tself::ERASE_BATCH
\t\t\t\t),
\t\t\t\tARRAY_A
\t\t\t); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\tif ( null === $rows_raw && '' !== (string) $wpdb->last_error ) { $messages[] = __( 'Continuity privacy erasure could not read the affected record set safely and will retry.', 'worldwide-clinic-appointments' ); $done = false; $rows_raw = array(); }
\t\t\t$rows = (array) $rows_raw;'''
              once(p,old3,new3)
              old4='''\t\t\t\t\t$still_exists = $wpdb->get_var( $wpdb->prepare( "SELECT id FROM {$table} WHERE id=%d AND {$field}=%d", $row_id, $user_id ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\t\t\tif ( $still_exists ) { $messages[] = __( 'Continuity privacy erasure could not remove an affected record and will retry.', 'worldwide-clinic-appointments' ); $done = false; break; }'''
              new4='''\t\t\t\t\t$still_exists = $wpdb->get_var( $wpdb->prepare( "SELECT id FROM {$table} WHERE id=%d AND {$field}=%d", $row_id, $user_id ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared
\t\t\t\t\tif ( null === $still_exists && '' !== (string) $wpdb->last_error ) { $messages[] = __( 'Continuity privacy erasure could not verify a concurrent delete safely.', 'worldwide-clinic-appointments' ); $done = false; break; }
\t\t\t\t\tif ( $still_exists ) { $messages[] = __( 'Continuity privacy erasure could not remove an affected record and will retry.', 'worldwide-clinic-appointments' ); $done = false; break; }'''
              once(p,old4,new4)
              old5='''\t\t\tif ( $more ) {
\t\t\t\t$done = false;
\t\t\t} else {
\t\t\t\tdelete_transient( $cursor_key );
\t\t\t}'''
              new5='''\t\t\tif ( null === $more && '' !== (string) $wpdb->last_error ) {
\t\t\t\t$messages[] = __( 'Continuity privacy erasure could not verify completion safely and will retry.', 'worldwide-clinic-appointments' );
\t\t\t\t$done = false;
\t\t\t} elseif ( $more ) {
\t\t\t\t$done = false;
\t\t\t} else {
\t\t\t\tdelete_transient( $cursor_key );
\t\t\t}'''
              once(p,old5,new5)
              old6="\t\t\t\t$guardian_remaining = $wpdb->get_var( $wpdb->prepare( \"SELECT id FROM {$intake_table} WHERE guardian_user_id=%d LIMIT 1\", $user_id ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared\n\t\t\t\tif ( $guardian_remaining ) { $messages[] = __( 'Guardian continuity references remain linked and will retry.', 'worldwide-clinic-appointments' ); $done = false; }"
              new6="\t\t\t\t$guardian_remaining = $wpdb->get_var( $wpdb->prepare( \"SELECT id FROM {$intake_table} WHERE guardian_user_id=%d LIMIT 1\", $user_id ) ); // phpcs:ignore WordPress.DB.PreparedSQL.NotPrepared\n\t\t\t\tif ( null === $guardian_remaining && '' !== (string) $wpdb->last_error ) { $messages[] = __( 'Guardian continuity references could not be verified safely and will retry.', 'worldwide-clinic-appointments' ); $done = false; } elseif ( $guardian_remaining ) { $messages[] = __( 'Guardian continuity references remain linked and will retry.', 'worldwide-clinic-appointments' ); $done = false; }"
              once(p,old6,new6)
          elif r == 'R18':
              p='worldwide-clinic.php'; s=read(p); s=s.replace('Version: 1.2.14','Version: 1.2.15',1).replace("define( 'WCA_VERSION', '1.2.14' )","define( 'WCA_VERSION', '1.2.15' )",1); write(p,s)
              p='includes/class-wca-contracts.php'; once(p,"const RUNTIME_VERSION                 = '1.2.14';","const RUNTIME_VERSION                 = '1.2.15';")
              p='tests/release-package-contract.php'; s=read(p).replace('1.2.14','1.2.15'); write(p,s)
              p='tests/fourteenth-twenty-review-regressions.php'; s=read(p).replace("t14has('R18 plugin 1.2.14','worldwide-clinic.php','Version: 1.2.14');","t14has('R18+ current plugin release remains aligned','worldwide-clinic.php','Version: 1.2.15');").replace("t14has('R18 runtime 1.2.14','includes/class-wca-contracts.php',\"RUNTIME_VERSION                 = '1.2.14'\");","t14has('R18+ current runtime release remains aligned','includes/class-wca-contracts.php',\"RUNTIME_VERSION                 = '1.2.15'\");"); write(p,s)
              p='readme.txt'; s=read(p); s=s.replace('Stable tag: 1.2.14','Stable tag: 1.2.15',1).replace('Version 1.2.13 implements','Version 1.2.15 implements',1).replace('CI-generated File 08 v1.2.13 candidate','CI-generated File 08 v1.2.15 candidate',1); marker='= 1.2.13 ='; entry="= 1.2.15 =\n* Fifteenth fresh 20-round sequential corrective audit: SQL read failures in conflict/hold/capacity/policy/dedupe/privacy/retention/concurrency gates now fail closed; release identity advanced without schema inflation.\n* R19-R20 are fresh corrected-state closure reviews; staging/live evidence remains separate.\n\n= 1.2.14 =\n* Fourteenth fresh 20-round corrective closure plus post-main heatmap privacy/timezone and deterministic public-cursor corrections.\n* Runtime 1.2.14; schemas remained core 3.2.0 / continuity 1.1.0 / Future24 1.0.0.\n\n"; s=s.replace(marker,entry+marker,1); write(p,s)
              p='CHANGELOG.md'; s=read(p); marker='## 1.2.14 — 2026-08-12'; entry="## 1.2.15 — 2026-08-13\n\n- Fifteenth fresh sequential 20-round review/fix/retest cycle hardened fail-closed SQL reads across appointment conflicts, active holds, slot-hold replay/conflict checks, rate limits, Future24 resource/group/policy/external-busy/dedupe flows, privacy/retention, and continuity optimistic concurrency.\n- Added permanent T15 regression evidence. Runtime 1.2.15; schemas unchanged at core 3.2.0 / continuity 1.1.0 / Future24 1.0.0. Repository QA remains distinct from staging/live acceptance.\n\n"; s=s.replace(marker,entry+marker,1); write(p,s)
              p='STATUS.md'; s=read(p); s=s.replace('Runtime candidate: **1.2.14**','Runtime candidate: **1.2.15**',1).replace('evidence for the v1.2.14 candidate','evidence for the v1.2.15 candidate',1); s += "\n\n## Fifteenth fresh 20-round corrected-state closure\n\n- Frozen repository product baseline: runtime 1.2.14 on the Fourteenth corrected branch state; the temporary audit probe was repository evidence only.\n- T15-R01 through T15-R18 corrected supported repository defects/gaps sequentially, each followed by the full source-level QA suite before the next review.\n- T15-R19 and T15-R20 are fresh corrected-state closure reviews with no new supported product defect.\n- Runtime: 1.2.15; core schema 3.2.0; continuity 1.1.0; Future24 1.0.0.\n- Staging-Accepted: false; Live-Deployed: unverified; Operational: not claimed.\n"; write(p,s)
              test='''<?php
$root=dirname(__DIR__); $pass=0; $fail=array();
function t15has($label,$path,$needle){global $root,$pass,$fail;$s=file_get_contents($root.'/'.$path);if(is_string($s)&&false!==strpos($s,$needle)){echo 'PASS '.(++$pass).': '.$label."\\n";}else{$fail[]=$label.' missing: '.$needle;}}
t15has('R01 conflict reads fail closed','includes/class-swc-helpers.php','unreadable conflict set must never be treated as an empty schedule');
t15has('R02 active holds fail closed','includes/class-wca-service.php','unreadable hold ledger means availability is unknown');
t15has('R03 hold replay read failure','includes/class-wca-repository.php','wca_slot_hold_read_failed');
t15has('R03 hold overlap read failure','includes/class-wca-repository.php','wca_slot_conflict_query_failed');
t15has('R04 rate counter read failure','includes/class-swc-helpers.php','authoritative count cannot be read');
t15has('R05 resource capacity read failure','includes/class-wca-future24.php','wca_resource_capacity_read');
t15has('R06 group membership read failure','includes/class-wca-future24.php','wca_group_membership_read');
t15has('R06 group capacity read failure','includes/class-wca-future24.php','wca_group_capacity_read');
t15has('R07 policy read fail closed','includes/class-wca-future24.php','a policy read error is not equivalent');
t15has('R08 external busy read fail closed','includes/class-wca-future24.php','unreadable external-busy projection must block scheduling');
t15has('R09 waitlist dedupe read','includes/class-wca-future24.php','wca_waitlist_dedupe_read');
t15has('R09 window dedupe read','includes/class-wca-future24.php','wca_windows_dedupe_read');
t15has('R10 offer dedupe read','includes/class-wca-future24.php','wca_waitlist_offer_dedupe_read');
t15has('R11 arrival dedupe read','includes/class-wca-future24.php','wca_arrival_dedupe_read');
t15has('R12 participant dedupe read','includes/class-wca-future24.php','wca_support_participant_dedupe_read');
t15has('R13 virtual-room dedupe read','includes/class-wca-future24.php','wca_virtual_room_dedupe_read');
t15has('R14 policy pagination read','includes/class-wca-future24.php','wca_future24_policy_read');
t15has('R15 appointment privacy read','includes/class-wca-privacy.php','wca_privacy_appointment_read');
t15has('R15 future24 retention read','includes/class-wca-privacy.php','wca_retention_future24_read');
t15has('R16 intake retention read','includes/class-wca-continuity-secure.php','wca_intake_retention_read');
t15has('R16 followup retention read','includes/class-wca-continuity-secure.php','wca_followup_retention_read');
t15has('R17 intake version read','includes/class-wca-continuity-guards.php','wca_intake_version_read');
t15has('R17 consent read','includes/class-wca-continuity-guards.php','wca_consent_state_read');
t15has('R18 plugin version','worldwide-clinic.php','Version: 1.2.15');
t15has('R18 contract version','includes/class-wca-contracts.php',"RUNTIME_VERSION                 = '1.2.15'");
if($fail){fwrite(STDERR,"File 08 fifteenth twenty-round regression gate failed:\\n- ".implode("\\n- ",$fail)."\\n");exit(1);}echo 'File 08 fifteenth twenty-round regression assertions passed: '.$pass.'/'.$pass."\\n";
'''
              write('tests/fifteenth-twenty-review-regressions.php',test)
              p='tests/run-all.php'; s=read(p); old="'fourteenth-twenty-review-regressions.php' );"; new="'fourteenth-twenty-review-regressions.php', 'fifteenth-twenty-review-regressions.php' );"; 
              if old not in s: raise SystemExit('tests/run-all.php T15 insertion point missing')
              write(p,s.replace(old,new,1))
          else:
              raise SystemExit('Unknown round '+r)
          PY

          cat > FIFTEENTH-TWENTY-REVIEW-EVIDENCE.md <<EOF
          # File 08 — Fifteenth Fresh 20-Round Sequential Corrective Review

          ## Frozen repository reality

          - Launching audit-probe HEAD before this workflow commit: \`$(git rev-parse HEAD^)\`
          - Product/source parent before the temporary probe: \`$(git rev-parse HEAD^^)\`
          - Branch: \`codex/file08-new-governing-plans-completion-2026\`
          - Starting runtime: **1.2.14**
          - Starting schemas: core **3.2.0**, continuity **1.1.0**, Future24 **1.0.0**
          - Scope: repository/source review only. No staging/live equivalence is claimed.

          Each round below was run against the corrected state produced by the preceding round. A supported defect was corrected first; PHP syntax, the complete File 08 source suite, and JavaScript syntax were then required to pass before the next round began.
          EOF

      - name: Run R01-R20 sequentially
        shell: bash
        run: |
          set -euo pipefail
          run_tests() {
            find . -type f -name '*.php' -not -path './vendor/*' -print0 | xargs -0 -n1 php -l >/tmp/t15-php-lint.log
            php tests/run-all.php
            node --check assets/js/clinic.js
            node --check assets/js/continuity.js
            node --check assets/js/future24.js
          }
          commit_round() {
            local round="$1" note="$2" result="$3"
            run_tests
            printf '\n## %s — %s\n\n%s\n\nRetest: **PASS** — full PHP syntax + File 08 source suite + JavaScript syntax.\n' "$round" "$result" "$note" >> FIFTEENTH-TWENTY-REVIEW-EVIDENCE.md
            git add -A
            git commit -m "File 08 T15 ${round}: ${result}"
          }

          python /tmp/t15_patch.py R01
          commit_round R01 'Existing-appointment conflict SQL failure was cast to an empty result and could therefore fail open. The conflict reader now treats an unreadable conflict set as occupied/unsafe.' 'defect corrected — conflict query fail-closed'

          python /tmp/t15_patch.py R02
          commit_round R02 'Canonical active slot-hold lookup returned false on database read failure. Hold-ledger uncertainty now blocks availability rather than presenting a free slot.' 'defect corrected — active-hold read fail-closed'

          python /tmp/t15_patch.py R03
          commit_round R03 'Slot-hold idempotent replay reads and the inside-lock overlap read did not distinguish no-row from SQL failure. Both now return explicit reconciliation-safe errors on unreadable state.' 'defect corrected — hold replay/conflict reads'

          python /tmp/t15_patch.py R04
          commit_round R04 'Rate-limit ON DUPLICATE KEY updates could succeed while LAST_INSERT_ID() retrieval failed, which cast to zero and could under-enforce throttling. The authoritative counter read now fails closed.' 'defect corrected — rate-limit counter read'

          python /tmp/t15_patch.py R05
          commit_round R05 'Future24 multi-resource capacity COUNT() could be read as zero after a database error, permitting over-capacity reservation. Capacity uncertainty now returns 503.' 'defect corrected — resource capacity read'

          python /tmp/t15_patch.py R06
          commit_round R06 'Group-session duplicate membership and capacity reads could fail open. Existing membership is now read as a row with error detection, and capacity COUNT() must be trustworthy before join.' 'defect corrected — group membership/capacity reads'

          python /tmp/t15_patch.py R07
          commit_round R07 'Future24 buffer/travel policy reads treated database failure as absence, and branch lookup failure could skip travel-gap enforcement. Both policy paths now fail closed.' 'defect corrected — scheduling policy read bypass'

          python /tmp/t15_patch.py R08
          commit_round R08 'External-calendar busy projection returned no conflict when its SQL read failed. Unreadable external-busy state now blocks a scheduling result until reconciliation.' 'defect corrected — external busy read'

          python /tmp/t15_patch.py R09
          commit_round R09 'Waitlist and flexible-window semantic de-duplication could create duplicates when their existing-record query failed. Both reads now return explicit safe errors.' 'defect corrected — waitlist/window dedupe reads'

          python /tmp/t15_patch.py R10
          commit_round R10 'Cancellation waitlist offer de-duplication treated an unreadable pending-offer query as no duplicate. Offer creation now stops on read failure.' 'defect corrected — waitlist-offer dedupe read'

          python /tmp/t15_patch.py R11
          commit_round R11 'Digital arrival semantic de-duplication could create another arrival record after a database read failure. Current arrival state must now be readable.' 'defect corrected — arrival dedupe read'

          python /tmp/t15_patch.py R12
          commit_round R12 'Support/interpreter participant de-duplication could proceed after a failed existing-role read. Participant creation now fails closed on unreadable state.' 'defect corrected — participant dedupe read'

          python /tmp/t15_patch.py R13
          commit_round R13 'Virtual-room request de-duplication could emit a second room request if the existing request query failed. The request path now fails closed.' 'defect corrected — virtual-room dedupe read'

          python /tmp/t15_patch.py R14
          commit_round R14 'Paged Future24 policy/template reads cast SQL errors to an empty set; prerequisite evaluation could therefore become falsely complete. The iterator now returns WP_Error and questionnaire/prerequisite/readiness callers propagate it.' 'defect corrected — policy pagination false-complete'

          python /tmp/t15_patch.py R15
          commit_round R15 'Base/File24 privacy erasure and Future24 retention could mistake failed SQL reads for completion/empty data. Cursor and retention reads now expose failure and keep work retryable.' 'defect corrected — privacy/retention false completion'

          python /tmp/t15_patch.py R16
          commit_round R16 'Secure continuity retention cast failed intake/follow-up reads to empty lists and could falsely report successful maintenance. Both retention scans now fail explicitly.' 'defect corrected — continuity retention reads'

          python /tmp/t15_patch.py R17
          commit_round R17 'The operative continuity guard could bypass optimistic intake version enforcement after a DB read failure; consent and replacement-erasure verification also hid some read failures. Those paths now fail closed/retryable.' 'defect corrected — continuity concurrency/privacy reads'

          python /tmp/t15_patch.py R18
          commit_round R18 'Current release documentation/package assertions still identified the candidate as older 1.2.13/1.2.14 states after the new corrections. Runtime/docs/tests were aligned to 1.2.15 with schemas unchanged and a permanent T15 regression gate added.' 'defect corrected — release identity/documentation alignment'

          # R19: fresh adversarial corrected-state audit. No source modification is permitted unless a supported defect is proven.
          grep -RInE "\(array\)[[:space:]]*\$wpdb->get_(row|results|col|var)|\(int\)[[:space:]]*\$wpdb->get_var" includes > /tmp/t15-r19-sql-casts.txt || true
          grep -RInE "get_var\(|get_row\(|get_results\(|get_col\(" includes/class-swc-helpers.php includes/class-wca-service.php includes/class-wca-repository.php includes/class-wca-future24.php includes/class-wca-privacy.php includes/class-wca-continuity-secure.php includes/class-wca-continuity-guards.php > /tmp/t15-r19-sql-reads.txt
          commit_round R19 'Fresh adversarial scan rechecked authorization-sensitive scheduling, conflict/hold/capacity, de-duplication, privacy/retention and concurrency reads after R18. No additional supported product defect was proven by this round; remaining SQL reads are either fail-closed, informational/derived, or covered by existing owner checks.' 'clean corrected-state review'

          # R20: second fresh closure review plus removal of temporary audit tooling before the final retest.
          rm -f .github/workflows/t15-probe.yml .github/workflows/t15-sequential-corrective.yml
          test ! -e .github/workflows/t15-probe.yml
          test ! -e .github/workflows/t15-sequential-corrective.yml
          test -z "$(find . -type f \( -name '*.pem' -o -name '*.key' -o -name '.env' -o -name '*.p12' -o -name '*.b64' \) -print -quit)"
          test -z "$(find . -type l -print -quit)"
          test ! -e .gitmodules
          ! grep -RInE '(BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY|ghp_[A-Za-z0-9]{30,}|AKIA[0-9A-Z]{16})' --exclude-dir=.git .
          commit_round R20 'Second fresh corrected-state closure review rechecked runtime/contracts/docs/package parity, repository hygiene, JavaScript/PHP syntax and the complete permanent regression suite. Temporary T15 probe/driver workflows were removed before this retest. No new supported product defect was proven.' 'clean corrected-state review + temporary-tool cleanup'

          printf '\n## Cycle result\n\nDefect-bearing rounds: **R01–R18**. Clean closure rounds: **R19, R20**.\n\nRepository runtime after correction: **1.2.15**; schemas remain core **3.2.0**, continuity **1.1.0**, Future24 **1.0.0**. Staging/live remain unverified by this repository-only cycle.\n' >> FIFTEENTH-TWENTY-REVIEW-EVIDENCE.md
          run_tests
          git add FIFTEENTH-TWENTY-REVIEW-EVIDENCE.md
          git commit --amend --no-edit
          git push origin HEAD:codex/file08-new-governing-plans-completion-2026
